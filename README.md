# library_app_S2026
A repository for making a small nodejs server connected to a DB (library_db).
Learn basic associations between two models(entities from DB).

## Clone the project from here and start by installing node_modules to be able to run the app (npm install).
